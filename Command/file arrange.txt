find . -maxdepth 1 -type f | while read -r f; do y=$(date -r "$f" +%Y); m=$(date -r "$f" +%m-%B); e="${f##*.}"; e="${e,,}"; [ "$f" = "$e" ] && e="others"; mkdir -p "$y/$m/$e"; mv "$f" "$y/$m/$e/"; done
